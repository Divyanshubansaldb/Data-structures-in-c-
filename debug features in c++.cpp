// use run to start running your program 
// use b and line number or the function name to make a break point at that line
// use f at any point to see at which frame(line) you are on
// use list(l) to find next some set of line which would be executed after that point
